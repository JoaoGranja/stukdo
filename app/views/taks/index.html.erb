<h1>To Do</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>State</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @to_do.each do |tak| %>
      <tr>
        <td><%= tak.content %></td>
        <td><%= tak.state %></td>
        <td><%= link_to 'Show', tak %></td>
        <td><%= link_to 'Edit', edit_tak_path(tak) %></td>
        <td><%= link_to 'Destroy', tak, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td><%= link_to 'Mark as Doing', change_tak_path(tak, state: "doing"), method: :put %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h1>Doing</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>State</th>
      <th colspan="5"></th>
    </tr>
  </thead>

  <tbody>
    <% @doing.each do |tak| %>
      <tr>
        <td><%= tak.content %></td>
        <td><%= tak.state %></td>
        <td><%= link_to 'Show', tak %></td>
        <td><%= link_to 'Edit', edit_tak_path(tak) %></td>
        <td><%= link_to 'Destroy', tak, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td><%= link_to 'Mark as To Do', change_tak_path(tak, state: "to_do"), method: :put %></td>
        <td><%= link_to 'Mark as Done', change_tak_path(tak, state: "done"), method: :put %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h1>Done</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>State</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @done.each do |tak| %>
      <tr>
        <td><%= tak.content %></td>
        <td><%= tak.state %></td>
        <td><%= link_to 'Show', tak %></td>
        <td><%= link_to 'Edit', edit_tak_path(tak) %></td>
        <td><%= link_to 'Destroy', tak, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td><%= link_to 'Mark as Doing', change_tak_path(tak, state: "doing"), method: :put %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tak', new_tak_path %>
